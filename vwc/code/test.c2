import sys.crt;
import geo.rect;
import geo.point;

enum {OK, Cancel, Answer};

int x, y;

void print() {
	return 0;
}

bool test_switch() {
	switch(x[10]) {
	case 10:
		break;
	default:
		break;
	}
	return 1;
}

void main() {
	print();
	if(test_switch()+10)
		return;
	if(x <= 5)
		return x;
	return 10;
}