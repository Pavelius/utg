#include "main.h"

bool squad::is(condition_s v) const {
	//switch(v) {
	//case GoodState: return !injury;
	//case MiddleState: return wounded && wounded < count;
	//case BadState: return wounded >= count;
	//}
	return false;
}

void squad::getinfo(stringbuilder& sb) const {
}